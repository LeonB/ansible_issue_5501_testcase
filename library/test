#!/usr/bin/python -tt

# this is magic, see lib/ansible/module_common.py
#<<INCLUDE_ANSIBLE_MODULE_COMMON>>

module = AnsibleModule(argument_spec = dict())
module.exit_json(changed=False, result={'unicode': 'test'})
